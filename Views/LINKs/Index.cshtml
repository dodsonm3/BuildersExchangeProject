@model IEnumerable<BuildersExchangeProject.Models.LINK>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.URL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FILTER.FILTER_NAME)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.URL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FILTER.FILTER_NAME)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.URL_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.URL_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.URL_ID })
        </td>
    </tr>
}

</table>
